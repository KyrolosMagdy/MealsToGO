import styled from "styled-components/native";
import { StyledSafeAreaView } from "../../../components/utility/safeAreaComponent";

export const NoFavouritesArea = styled(StyledSafeAreaView)`
  align-items: center;
  justify-content: center;
`;
